## Routing

_Routing is a mechanism which allows react to navigate from one component to another component dynamically without reloading the page in the sync of URL._

_We can use routing using one third party library `react-router-dom`_

_In React, routers are used for managing navigation and changing the view based on the URL. The most popular router for React is `react-router-dom`._
